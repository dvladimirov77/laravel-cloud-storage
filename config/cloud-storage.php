<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Диск по умолчанию
    |--------------------------------------------------------------------------
    |
    | Должен совпадать с одним из дисков в config/filesystems.php
    | Типичные значения: 's3', 'spaces', 'minio', 'gcs'
    |
    */
    'default_disk' => env('CLOUD_STORAGE_DISK', 's3'),

    /*
    |--------------------------------------------------------------------------
    | Время жизни временных ссылок (в минутах) по умолчанию
    |--------------------------------------------------------------------------
    */
    'temporary_url_ttl' => env('CLOUD_STORAGE_TEMP_URL_TTL', 60),
];
